{{ range $service := .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $service.name }}
  namespace: {{ $.Release.Namespace }}
spec:
  selector:
    app: {{ $service.app }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
        
{{ println "---" }}          
{{- end }}